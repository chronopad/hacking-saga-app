import User from "../models/user.model.js";

export const searchUser = async (req, res) => {
    try {
        console.log("Temporary");
    } catch (error) {
        console.log("Temporary");
    }
}